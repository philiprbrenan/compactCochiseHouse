# Test 2026-09-12 at 02:08:28

name: compactCochiseHouse

on:
  push:
    paths:
      - '**/main.yml'

jobs:
  test
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Position files in package
      run: |
        perl floorPlan.pl
